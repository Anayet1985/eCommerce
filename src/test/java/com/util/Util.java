package com.util;

import java.io.FileInputStream;

import org.openqa.selenium.WebDriver;


public class Util {
	
	WebDriver driver;
	
	/*
	 * public void static dataExcel() { FileInputStream fis = new
	 * FileInputStream("path of excel file");
	 * 
	 * Workbook wb = WorkbookFactory.create(fis);
	 * 
	 * Sheet s = wb.getSheet("sheetName");//Everyone is class
	 * 
	 * String value = s.getRow(rowNum).getCell(cellNum).getStringCellValue(); }
	 */
}
